<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<select id = "findAll" resultType = "mybatis.vo.MemberVO">
		select * from member_t
	</select>
	
	<!-- 회원가입 -->
	<insert id="add" parameterType="java.util.Map">
  		INSERT INTO laptopmember(f_key, member_name, member_age, member_gender, email, profile_image, password)
  		VALUES(laptopmember_seq.NEXTVAL, #{member_name}, #{member_age}, #{member_gender}, #{email}, #{profile_image}, #{password})
  	</insert>
  	
  	<!-- 즐겨찾기 저장 -->
  	<insert id="addFavorite" parameterType="java.util.Map">
  		INSERT INTO favorite(f_key, laptop_seq)
  		VALUES(#{f_key}, #{laptop_seq})
  	</insert>
  	
  	<!-- 즐겨찾기 삭제 -->
  	<delete id="delFavorite" parameterType="java.util.Map">
  		DELETE FROM favorite WHERE f_key = #{f_key} AND laptop_seq = #{laptop_seq}
  	</delete>
  	
  	<!-- signInChk -->
  	<select id="signInChk" parameterType="java.util.Map" resultType="mybatis.vo.MemberVO">
  		SELECT * FROM laptopmember WHERE email = #{email} AND password = #{password}
  	</select>
  	
  	<!-- 즐겨찾기 불러오기 -->
  	<select id="getFavorite" parameterType="java.util.Map" resultType="mybatis.vo.FavoriteVO">
  		SELECT * FROM favorite WHERE f_key = #{f_key} AND laptop_seq = #{laptop_seq}
  	</select>
</mapper>